#include <stdio.h>

int main(){
    short  contador = 0;
    short  condicion = (contador % 2 == 0);
    
    if (condicion) {
        printf("%d \t --->  Es un Numero Par \n", contador);
        contador++;
    }

    if (condicion) 
        contador++;

    if (condicion) {
        printf("%d \t --->  Es un Numero Par \n", contador);
        contador++;
    }

    if (condicion) 
        contador++;

    if (condicion) {
        printf("%d \t --->  Es un Numero Par \n", contador);
        contador++;
    }

    if (condicion) 
        contador++;

    if (condicion) {
        printf("%d \t --->  Es un Numero Par \n", contador);
        contador++;
    }

    if (condicion) 
    contador++;

    if (condicion) {
        printf("%d \t --->  Es un Numero Par \n", contador);
        contador++;
    }

    if (condicion) 
        contador++;

    if (condicion) {
        printf("%d \t --->  Es un Numero Par \n", contador);
        contador++;
    }

    if (condicion) 
        contador++;

    if (condicion) {
        printf("%d \t --->  Es un Numero Par \n", contador);
        contador++;
    }

    if (condicion) 
        contador++;

    if (condicion) {
        printf("%d \t --->  Es un Numero Par \n", contador);
        contador++;
    }

    if (condicion) 
    contador++;

    if (condicion) {
        printf("%d \t --->  Es un Numero Par \n", contador);
        contador++;
    }

    if (condicion) 
        contador++;

    if (condicion) {
        printf("%d \t --->  Es un Numero Par \n", contador);
        contador++;
    }

    if (condicion) 
        contador++;

    if (condicion) {
        printf("%d \t --->  Es un Numero Par \n", contador);
        contador++;
    }

}